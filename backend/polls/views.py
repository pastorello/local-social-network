# Legacy Django-tutorial template views (DetailView, ResultsView, vote) were
# removed in M0: their templates never existed in this repo, so every route
# returned a 500. The poll voting flow is rebuilt as a DRF API in M3
# (see docs/mvp-spec.md §4-F3 and §8).
